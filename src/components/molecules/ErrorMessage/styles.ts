import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  subTitle: {fontWeight: 'normal', color: '#DDD', fontSize: 14, padding: 8},
  outroContainer: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default styles;
